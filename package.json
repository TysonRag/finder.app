{
  "name": "fundfinder-app",
  "version": "1.0.0",
  "description": "Android wrapper for FundFinder SA",
  "private": true,
  "scripts": {
    "build-android": "npx cap add android && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
